# example project `(clj-workos)`

## run

We have an alias in `deps.edn` called `run`

```sh
WORKOS_API_KEY=... \
    WORKOS_CLIENT_ID=... \
    WORKOS_REDIRECT_URL=http://... \
    clojure -X:run
```
